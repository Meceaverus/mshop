{% extends 'base.html.twig' %}

{% block title %}
    О нас
{% endblock %}


{% block body %}
    <h1>Здесь может быть ваша реклама!</h1>
    <a href="{{ notebooksUrl }}">Notebooks</a>
    <a href="{{ path('category_show', {'slug': 'tvs', 'page': 5}) }}">TVset</a>
    <a href="{{ path('category_show', {'slug': 'media'}) }}">Media centers</a> <br>

    Last visited category: {{ lastCategory }}
{% endblock %}